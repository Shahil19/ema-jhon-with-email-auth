/**
 * 1. Create a new project in console.google.com
 * 2. npm install firebase
 * 3. initialize firebase (firebase.init.js) and create *const = getAuth(app)* then export auth
 * 4. firebase project > authentication > sign in method (email and pass)
*/