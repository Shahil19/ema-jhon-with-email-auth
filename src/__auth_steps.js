/**
 * 1. Create a new firebase project in console.google.com
 * 2. npm install firebase
 * 3. initialize firebase (firebase.init.js) and create *const = getAuth(app)* then export auth
 * 4. firebase project > authentication > sign in method (email and pass)
 * 5. Create <Login> and <SignUp> Components , setup routes
 * 6. Attach form field handler(input handler) and form submit handler
 * 7. npm install --save react-firebase-hooks (web: search for react firebase hooks github)
 * 8. Authentication hooks in readme file (web: search for react firebase hooks github)
 * 9. useCreateUserWithEmailAndPassword form react-firebase/auth
 * 10. if user is created redirect to expected page (Component <SignUp> line 27)
*/

// Private page (Routing Authentication)
/**
 * 1. Component <RequireAuth>
 * 2. in Stick-blitz line 130 (function RequireAuth )
 *  
 * */